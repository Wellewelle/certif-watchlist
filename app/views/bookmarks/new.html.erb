<h1>Bookmarks new</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.association :movie %>
  <%= f.submit %>
<% end %>

<%= link_to "Back", list_path(@list) %>
